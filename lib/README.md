# SQLite JDBC Driver

Place the SQLite JDBC driver here before running the application:

```text
lib/sqlite-jdbc.jar
```

Recommended artifact name:

```text
sqlite-jdbc-3.45.3.0.jar
```

You can rename the downloaded JAR to `sqlite-jdbc.jar` so the command-line examples in the main README work as written.
